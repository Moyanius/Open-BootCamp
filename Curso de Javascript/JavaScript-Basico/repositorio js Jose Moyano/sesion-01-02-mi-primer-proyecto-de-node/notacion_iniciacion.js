// Notacion

// ; --> Delimita el final de una linea, aunque se puede eliminar
const b = 4;

b + 4;

// . --> Se utiliza en los objetos para acceder a los atributos
// movil.anchura 

// [] --> Se utiliza para los arrays
const ar = [1, 2, 3, 4];
console.log(ar[2]);

// () --> Funciones
function suma(a, b){
    //se escribe la funcion   
}

// {} --> Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if (true){
    // todo lo que este entre llaves
    const constante2 = "hola";
}